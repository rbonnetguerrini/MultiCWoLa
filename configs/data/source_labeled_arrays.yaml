kind: arrays
dataset_name: source_labeled_arrays
path: ???

# Required when the NPZ file does not include `pi`, `source_given_class`, or
# true posterior arrays.
K: ???

# Optional. If omitted, inferred as max(train_source) + 1.
M: null

task_type: auto
flatten: false
