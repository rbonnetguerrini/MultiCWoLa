defaults:
  - galaxy10_m20

# Purity sweep cell (K=10, M=20), p=0.90: pi_m ~= 0.10*(1/K) + 0.90*e_{c(m)}.
# Near-pure mixtures (each source dominated by one class) -- easy regime.
pi_mode: dirichlet
pi_dirichlet_alpha: 1000.0
condition_strength: 0.9
